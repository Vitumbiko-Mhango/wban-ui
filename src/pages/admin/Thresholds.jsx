import React from "react";
import Heading from "../../components/common/Heading";

const Thresholds = () => {
  return (
    <div>
      <Heading
        title="Alert Thresholds"
        subtitle="Define safe ranges for each vital sign. Readings outside these limits will trigger an alert."
      />
    </div>
  );
};

export default Thresholds;
